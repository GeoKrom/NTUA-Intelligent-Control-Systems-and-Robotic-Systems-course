function out = NN(x)
    % Neural Network RBF Summary of this function goes here
    %   Detailed explanation goes here
    K = 5;

    mi = zeros(K);
    sigma = zeros(K);
    w = zeros(K);
    b = ones(K);
    lr = 0.01;
    epoch = 500;


    for i=1:epoch-1
        
    end
end